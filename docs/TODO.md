# Ragnarok TODO

Last updated: 2026-05-29

This is the single living todo for Ragnarok. Tasks stay here after completion and are updated with their status and fulfillment notes rather than being removed or replaced.

## Status scale

- `Open`: not started or still incomplete
- `In progress`: currently being implemented
- `Done`: completed and should remain documented here with fulfillment notes
- `Not Needed`: deliberately not pursued; kept here with the rationale so it is not re-proposed

## Priority scale

- `Critical`: blocks trustworthy project exchange or core modeling correctness
- `High`: major product gap or frequent user pain
- `Medium`: meaningful support expansion or UX improvement
- `Low`: polish or secondary capability

## Cost scale

`Estimated token cost` is a rough implementation budget for one focused coding pass, including reading, patching, verification, and light documentation. It is not a calendar estimate.

## Surface scale

- `Frontend`: can be completed without backend code changes
- `Backend`: backend-only change
- `Both`: requires coordinated frontend and backend work

## Todo

| Status | Priority | Surface | Area | Task | Why it matters | Estimated token cost | Fulfillment / notes |
|---|---|---|---|---|---|---:|---|
| `Done` | `Critical` | `Both` | Optimization capability | Add multi-investment / pathway planning support | This was one of the largest gaps between PyPSA and Ragnarok. | 35,000 | Fulfilled with an opt-in pathway mode, shared pathway config metadata sheets, backend multi-investment expansion into PyPSA, period-aware outputs/results, selected-period analytics, and a maintained pathway sample workbook. Polished in PR #15: pathway settings moved to a dedicated **Multi-year planning** sidebar group, planning-mode pill selector moved out of the run dialog, `_snapshots_index` / `_apply_ts_sheet` dedupe so single-period runs on pathway-style workbooks no longer hit "Reindexing only valid with uniquely valued Index objects", and the period selector switched to pill buttons matching the rest of the UI. |
| `Done` | `High` | `Frontend` | Analytics | Add a capacity-over-investment-periods chart for pathway runs | Pathway users had no way to see how installed capacity evolves across the planning horizon. | 10,000 | Fulfilled in PR #16 + follow-ups: `CapacityByPeriodCard` renders a stacked column chart of total active capacity per investment period, grouped by carrier or by generator. Active capacity respects `build_year` / `lifetime`; extendable assets use `p_nom_opt`, fixed assets use `p_nom`. Driven entirely off `(model, outputs)` so it works on imported projects with no backend call. The new-additions/bin variants were removed in favour of letting the Capacity Expansion table below handle the "what was newly built" question. |
| `Not Needed` | `Critical` | `Backend` | Project export/import | Retain the solved backend `pypsa.Network` or solved workbook artifact after a run | A solved-network artifact would complement the current pure-JSON path for users who want PyPSA-native round-trips (Python notebooks, downstream tooling). Current export is frontend-assembled from `results.outputs` — accurate but not the authoritative `pypsa.Network`. | 18,000 | **Decided Not Needed (2026-05-29):** Ragnarok's server is intentionally stateless. Holding per-run solved-network state on the backend introduces statefulness, memory pressure, and run-lifecycle/cleanup concerns that do not belong in a server-based tool. The JSON/workbook output cache already round-trips losslessly on the frontend (PR #9) and `deriveRunResults` rebuilds the full `RunResults` on import without a backend call (PR #11), so backend retention buys nothing for Ragnarok-internal trust. Users who need a native `pypsa.Network` can reconstruct one from the exported workbook/CSV-folder. |
| `Done` | `Critical` | `Frontend` | Project export/import | Add Ragnarok metadata sheets for settings, constraints, run window, run history, and import provenance | Current project import restores more result metadata than before, but users still do not get the full project state back. | 20,000 | Fulfilled with dedicated workbook metadata sheets for settings, constraints, run state, and provenance. `Export Project` writes those sheets; `Import Project` restores them before rebuilding result state. Verbose run history was added then deliberately removed: it was redundant because `deriveRunResults` already rebuilds the current run from output sheets on import, and storing full result payloads per entry caused Excel 32767-character cell limit failures. The current run is synthesized as a single `Import N` entry on import; prior run history is not preserved across project export/import. |
| `Done` | `Critical` | `Both` | Project export/import | Switch project export/import to a pure-JSON pipeline driven by the PyPSA schema | The backend used to write an xlsx artifact then ship it back; this fought the schema and lost outputs on edge cases. | 22,000 | Fulfilled in PR #9: backend returns a schema-driven `outputs.{static,series}` cache; frontend assembles the project workbook locally by merging input rows with output static columns and appending output series sheets. Sidebar gains separate **Import Project / Export Project / Export Result** buttons; Demo button removed. |
| `Done` | `High` | `Frontend` | Architecture | Derive per-asset detail records on the frontend instead of the backend | Per-asset details (generator outputSeries / busDetail / branchDetail / storage SoC) were Python-only, which prevented Analytics from working after an import and duplicated math vs the raw schema cache. | 15,000 | Fulfilled in PR #9 follow-up: `deriveAssetDetails` walks the cached `outputs` plus `model` carriers/colors. Backend `lib/results/assets/` removed. Plugins and Analytics now read from a single in-memory source. |
| `Done` | `High` | `Frontend` | Import fidelity | Rebuild the full `RunResults` (summary, dispatch series, cost breakdown, carrier mix, nodal balance, line loading, merit order, emissions breakdown, expansion results, asset details) on the frontend when importing a project workbook | Imports previously left the Result tab empty unless the user re-ran PyPSA. | 18,000 | Fulfilled in PR #11 via `deriveRunResults`. Import also creates an `Import N` run-history entry so imported cases appear in the sidebar and Comparison tab. `co2Shadow` remains the one field that needs a fresh solve. |
| `Done` | `Medium` | `Frontend` | Reporting | Add an HTML "Export Report" action that emits a self-contained shareable result file | Users wanted a one-click way to share or print a run summary without sending an xlsx. | 8,000 | Fulfilled in PR #12: standalone .html with inline CSS + inline SVG (stacked-area dispatch, system price line, emissions line), KPI cards, cost breakdown, expansion, merit order, nodal balance, line loading, emissions breakdown, and the solver narrative. No external scripts. |
| `Done` | `Medium` | `Both` | Architecture | Schema-drive run-history counts and the backend "Imported model" log line | The last hardcoded component lists in IO-adjacent code. | 4,000 | Fulfilled in PR #10: `RunHistoryEntry.componentCounts` is now `Record<string, number>` populated by walking `SHEETS`; backend log iterates `network.components.keys()`. New PyPSA components flow in automatically after a schema regen. |
| `Done` | `Medium` | `Frontend` | Run UX | Simplify the run configuration modal and unify it with the rest of the app | The run dialog was a wall of inline labels, range sliders, and mode-conditional explanation blocks that did not match the validation pane or sidebar. | 6,000 | Fulfilled in two phases. First, the dialog was normalized to the Validation-pane visual language and the detailed planning editors were moved into sidebar groups (`Simulation window`, `Multi-year planning`, `Rolling horizon`). Second, scenario presets made the sidebar the only planning editor, so the run dialog now acts as an execution summary plus the small execution-only toggles (`Force LP`, `Dry run`). |
| `Done` | `High` | `Both` | Optimization capability | Add rolling-horizon optimization workflow | PyPSA supports rolling-horizon style operation, but Ragnarok only supports a single static solve window today. | 18,000 | Fulfilled: backend stitches windows via `network.optimize.optimize_with_rolling_horizon` (horizon + overlap configurable); sidebar **Rolling horizon** group owns the horizon and overlap inputs; run dialog has a single toggle. Independent of single-period vs pathway mode. |
| `Done` | `High` | `Both` | Optimization capability | Add stochastic optimization support | PyPSA supports two-stage stochastic planning; Ragnarok has no scenario or uncertainty model. | 30,000 | Fulfilled with `backend/pypsa/stochastic.py` (parse → `set_scenarios` → apply per-scenario `load_multiplier` overrides → solve → per-scenario summary → collapse to highest-weight scenario so the existing result pipeline keeps working unchanged). Sidebar gains a **Stochastic uncertainty** group with per-scenario rows (name / weight / load×). Weights are normalised to sum=1 at solve time; minimum 2 scenarios required (single-scenario configs fall back to deterministic). Combining with rolling horizon is rejected with HTTP 400. New `StochasticScenariosCard` in the Result dashboard shows per-scenario energy / emissions / operating cost / load shedding plus a probability-weighted "Expected" row; the representative scenario (highest weight) is highlighted as the one whose dispatch is shown in every other chart. Tests in `backend/tests/test_stochastic.py` pin: per-scenario totals, weight normalisation, single-scenario fallback, and the stochastic + rolling rejection. |
| `Done` | `High` | `Both` | Optimization capability | Add security-constrained optimization / SCLOPF workflow | Important missing transmission-planning capability for serious network studies. | 24,000 | Fulfilled by routing the solve through `network.optimize.optimize_security_constrained(...)` when `securityConstrainedConfig.enabled` is set. Defaults to N-1 security against every passive branch (line + transformer) — PyPSA generates per-branch outage constraints. Sidebar **Security-constrained (SCLOPF)** group exposes a Mode toggle and an N-1 coverage readout (line count + transformer count). Cannot be combined with rolling horizon, stochastic mode, or pathway mode — rejected at the API layer with HTTP 400. Result dashboard prefixes a banner explaining that the line-loading table values are worst-case N-1 loadings. Pinned by `backend/tests/test_sclopf.py` (3-bus triangle network: without N-1 the cheapest dispatch loads L3 to 75%; with N-1 every line stays ≤ 50%). |
| `Done` | `High` | `Frontend` | Import fidelity | Restore plugin analytics and non-derivable solve metadata on project import | Imported projects currently lose plugin analytics and CO2 shadow information. | 10,000 | Fulfilled in the import-metadata follow-up PR: project workbooks now carry `RAGNAROK_ResultMeta` and `RAGNAROK_PluginAnalytics` sheets, and import restores `pluginAnalytics`, `co2Shadow`, `runMeta`, `pathway`, and solver narrative without any backend round-trip or plugin re-execution. |
| `Done` | `High` | `Both` | Validation | Expand validation from common electricity checks to schema-aware coverage | The input schema is broad, but validation still focuses on buses, loads, generators, lines, and links. | 12,000 | Fulfilled with schema-driven frontend and backend validation. Local input checks now cover all schema-defined component sheets with generic duplicate-name, required-field, bus-reference, carrier-reference, numeric sanity, and time-series column/row checks. Backend dry-run validation now operates over the shared schema catalogue, accepts documented sheets generically, expands network summary counts, and reports pathway / rolling horizon effective-horizon notes plus ignored output-sheet/input-column notes. |
| `Open` | `High` | `Both` | Data platform | Add location-based wind and solar profile generation/import | Users should be able to choose a location and populate renewable profiles without preparing time series manually. This needs source selection, caching/database design, and workbook write-back into PyPSA time-series sheets. | 22,000 | |
| `Open` | `High` | `Backend` | Data platform | Add a renewable/weather profile database layer | Location-based profiles should not be one-off fetches only. Ragnarok needs persistent storage or cache/indexing for reusable wind/solar resources by location, year, and source. | 18,000 | |
| `Open` | `High` | `Both` | Data platform | Add national-level starter model imports | Users should be able to import a prebuilt country-scale baseline model instead of constructing one workbook from scratch. | 24,000 | |
| `Open` | `High` | `Frontend` | Optimization UX | Add an annual demand-growth generator that expands a base load profile into a period-stamped multi-year `loads-p_set` | Multi-year / pathway runs need demand that grows across investment periods, but there is no tool to produce the pattern — users must hand-build a period-stamped `loads-p_set` for every year. The plumbing already exists: in `explicit_period_column` mode the backend turns a `period` column on any time-series sheet into a `(period, snapshot)` MultiIndex and feeds PyPSA's investment-period machinery (`backend/pypsa/network/components.py:121-145`, `backend/pypsa/network/snapshots.py`), so per-year demand is just the same timestamps emitted once per period with values scaled differently each period. This is therefore a **frontend-only generator**: read the base-year `loads-p_set` profile plus the pathway period years (`RAGNAROK_PathwayPeriods` via `src/shared/utils/pathway.ts`), apply a growth pattern, and write back the expanded sheet as one undoable mutation (reusing the existing `onImportTsSheet` / bulk-paste path). Natural home is the Build **Load** step's temporal area, next to the new Import CSV / Paste block controls. Should set `snapshotMappingMode = explicit_period_column` and warn if pathway mode/periods are not configured. Open decisions deferred to implementation: **growth granularity** (single global %/yr vs per-load vs per-carrier/sector); **growth shape is user-defined** — favour explicit per-period multipliers / factors the user enters per year rather than hardcoding a CAGR or linear formula (a formula helper can pre-fill the per-period values but the user can override). | 14,000 | |
| `Open` | `High` | `Backend` | Data platform | Add a source registry for national models and profile datasets | National model import and profile generation need a managed catalogue of sources, versions, coverage, and provenance. | 16,000 | |
| `Done` | `High` | `Frontend` | Optimization UX | Add scenario and multi-period run configuration UX | Multi-year and stochastic support are not useful without a first-class configuration surface. | 18,000 | Fulfilled with a workbook-backed scenario manager in the sidebar. Users can capture the current run window, carbon price, pathway, rolling-horizon, load-shedding, force-LP, and active constraint setup into named presets, apply them back into the live frontend state, and keep those presets in normal save/project export flows without any backend API change. The run dialog now acts as an execution summary surface rather than a second planning editor. |
| `Done` | `High` | `Frontend` | Analytics | Add multi-period and scenario-aware result analytics | Pathway/stochastic support requires different result views, comparisons, and summaries. | 20,000 | Pathway side fulfilled earlier (period KPI strip, period summary table, capacity-over-periods chart, comparison-table pathway period rows). Cross-scenario side fulfilled by `ScenarioPivotCard` in the Comparison tab: groups included run-history entries by `scenarioLabel`, picks the latest run per scenario, and pivots Total dispatch / Emissions / Average price / Peak load into a metrics × scenarios table. Δ columns show absolute and percentage difference versus the leftmost scenario; the active run is highlighted. Renders only when ≥ 2 distinct scenarios are present (otherwise the existing per-run RunComparisonTable is sufficient). |
| `Done` | `High` | `Frontend` | Analytics | Add workbook-import restoration for more solve-derived metrics beyond current derivation set | Imported project analytics should match a fresh solve more closely. | 12,000 | Fulfilled in PR #11 — `deriveRunResults` rebuilds summary, dispatch series, cost breakdown, carrier mix, nodal balance, line loading, merit order, emissions breakdown, expansion results, and asset details from `(model, outputs)`. |
| `Done` | `Medium` | `Both` | Workbook model | Add regression tests and docs for backend import contract | Lock down the rule that `network` uses explicit runtime import, `snapshots` stays a snapshot-index special case, and all other schema-defined sheets use the generic schema-driven import path. | 7,000 | Fulfilled by `backend/tests/test_import_contract.py` (first Python test suite in the repo) and `backend/pypsa/network/README.md`. The 8 tests pin the three-rule contract: (1) `network` sheet applies only fields whitelisted by `network_runtime_import_fields()` — unknown columns are ignored; (2) `snapshots` sheet produces a flat `DatetimeIndex` in single-period mode and a `pd.MultiIndex(period, timestep)` under pathway planning; (3) every other schema-defined sheet (processes, line_types, generators incl. time-series sheets via `<list_name>-<attr>` convention, broken bus-ref cleanup) flows through the generic `_ordered_component_sheets` → `input_static_attributes` → `network.add()` loop with no per-sheet branches. Run with `.venv-pypsa/bin/python -m pytest backend/tests/ -v`. |
| `Done` | `Medium` | `Frontend` | Components | Add dedicated result UX for `processes` | Backend and workbook support exist, but there is no analytics surface. | 12,000 | Fulfilled by `ProcessDetail` type, `buildProcesses()` in `deriveAssetDetails.ts`, `process` focus type extension across `MapDetailCard`, `useMetricOptions`, `UserDefinedChartCard`, and `AnalyticsPane.focusSummary`. KPIs: power rating (input or solved `p_nom_opt`), throughput (∑|p0|·dt), throughput-weighted realised efficiency (|p1|/|p0|), operating cost. Sparkline tracks throughput |p0|; chart-section options expose throughput and the p0/p1 terminal-flow pair. Backend `full_outputs.py` falls back to `comp.dynamic` when `network.<list_name>_t` does not exist (the case for `processes` in PyPSA 1.2+), with regression test `backend/tests/test_full_outputs.py`. |
| `Done` | `Medium` | `Frontend` | Components | Add dedicated result UX for `shunt_impedances` where useful | Currently workbook/backend only. | 8,000 | Fulfilled by `ShuntImpedanceDetail` type, `buildShuntImpedances()` in `deriveAssetDetails.ts`, and a new `shuntImpedance` focus type wired through `MapDetailCard`, `useMetricOptions`, `UserDefinedChartCard`, and `AnalyticsPane.focusSummary`. KPIs: conductance g (S), susceptance b (S) with capacitive/inductive interpretation, peak |P| and peak |Q|. Chart metrics expose active power (`p`, MW) and reactive power (`q`, MVar) time series. Sparkline tracks reactive power Q. |
| `Done` | `Medium` | `Both` | Types and standard types | Clarify and test `line_types` / `transformer_types` behavior end-to-end | These are currently mostly implicit/pass-through capabilities. | 7,000 | Fulfilled by: (1) backend round-trip tests `backend/tests/test_type_references.py` that build a network from a workbook containing both a `line_types` row and a `lines` row referencing it (same for transformers) and assert both land in the network with `network.lines.at[..., "type"]` resolved; (2) frontend validation rule `addTypeReferenceIssues` in `useModelIssues.ts` — accepts any name in `model.line_types` / `model.transformer_types` **or** in the PyPSA built-in catalogues (PyPSA pre-populates `network.line_types` / `.transformer_types` on every fresh network, so workbook entries are not required); (3) `<datalist>`-backed typeahead in the input table grid for `lines.type` / `transformers.type` cells, sourced from the same merged catalogue. The earlier standalone "Component types" workspace overlay + sidebar group was removed in favour of editing inline. |
| `Done` | `Medium` | `Both` | Constraints | Expand the custom constraint panel or better expose native `global_constraints` behavior | Constraint authoring is stronger in the workbook/backend than in the UI. | 9,000 | Fulfilled by `ConstraintsWorkspaceView` (full-width overlay opened from a new sidebar shortcut, ESC-dismissable). Two tabs: **Custom** retains the existing UI-authored linopy constraints; **Global constraints** is a table editor for the PyPSA-native `global_constraints` sheet (name, type, sense, constant, carrier_attribute, investment_period, bus) covering primary_energy, transmission_volume_expansion_limit, transmission_expansion_cost_limit, operational_limit, tech_capacity_expansion_limit. Sidebar Constraints group replaced with a compact summary + "Open editor →" button. Backend correctness fix: `_drop_broken_bus_refs` now consults the schema's `bus_reference_attributes(...required=True)` filter, so global_constraints rows without a bus column no longer get silently dropped (regression test `test_generic_loop_imports_global_constraints_without_bus`). Introduces the workspace-overlay pattern (`activeWorkspaceOverlay` in App.tsx) reusable by future complex editors. |
| `Done` | `Medium` | `Both` | Testing | Add round-trip tests for `Save`, `Export Project`, `Import Project`, and `Export Result` | Current support is powerful enough that regression risk is now high. | 14,000 | Fulfilled (first automated test suite in the repo) in `src/shared/utils/workbook.test.ts`: 9 Jest tests covering `buildProjectWorkbook` → `parseProjectWorkbook` for input static sheets, output static + series (asserting outputs are not destructively merged into inputs), settings type-preserving round-trip, constraints, >40k-char payload chunk/reassembly, "run history is not exported", input-date ISO normalization, and input-only `Save` (`workbookToArrayBuffer` + `parseSheets`). Run with `CI=true npx react-scripts test --watchAll=false`. Note: `@types/jest` is not installed, so test files import `{ describe, test, expect }` from `@jest/globals`. Remaining gap: the `Export Result` reporting workbook / HTML report are not yet asserted. |
| `Open` | `Medium` | `Both` | Data platform | Add data-source health checks and provenance reporting | Imported profiles and national baseline models should expose where they came from, which version was used, and whether the source is still reachable. | 12,000 | |
| `Open` | `Medium` | `Frontend` | Data platform | Add user-facing data import presets by country/region | A curated “import baseline model for country X” flow reduces setup effort and keeps national models consistent. | 14,000 | |
| `Done` | `Medium` | `Frontend` | File formats | Support PyPSA CSV folder import/export in the UI | Useful for advanced users and larger cases. | 15,000 | Fulfilled by `src/shared/utils/csvFolder.ts` (uses `fflate` for in-browser zip). Two new File-group buttons: **Import CSV folder** (zipped) and **Export CSV folder**. The export packs every non-empty schema-known sheet — including `<list_name>-<attr>` time-series sheets — into `<archive>/<sheet>.csv`; the import resolves filename → sheet by stripping `.csv`, skipping anything not in the schema and reporting it as unknown. Numeric cells are coerced; everything else stays string. Confirmed PyPSA-native: `pypsa.Network().import_from_csv_folder(...)` reads the exported archive without modification. Pinned by `csvFolder.test.ts` (3 Jest cases: full round-trip, empty sheets skipped, unknown files reported). |
| `Done` | `Medium` | `Both` | File formats | Support netCDF/HDF5 workflows in the UI | Better aligned with PyPSA's broader I/O model than Excel-only workflows. | 18,000 | Fulfilled with four backend endpoints — `POST /api/export/{netcdf,hdf5}` (build a network from the posted model and stream the file back) and `POST /api/import/{netcdf,hdf5}` (parse via `pypsa.Network.import_from_*` and return the in-memory model JSON). Frontend exposes Import / Export buttons for both formats in the File sidebar group. Backend deps `netCDF4` and `tables` added to `backend/requirements.txt`. Conversion is server-side because the browser has no mature netCDF/HDF5 readers. Pinned by `backend/tests/test_binary_io.py` (parametrised round-trip: bus + generator identities preserved through both formats). |
| `Done` | `Low` | `Frontend` | Analytics | Add a dedicated carrier-level analytics view | Carrier data exists indirectly today. | 6,000 | Rebuilt as `CarrierAnalysisCard` after the first revision was judged redundant with the energy-mix donut. The new version focuses on **derived operational metrics** the existing cards don't show: capacity factor (energy / (capacity × modelled hours)), curtailment ratio (Σ curtailed / Σ available), effective cost per MWh, and emissions intensity per carrier. Load-shedding rows excluded. Surfaced as a collapsible section in the Result dashboard (default collapsed to keep it from crowding the headline charts). Pinned by `CarrierAnalysisCard.test.ts` (2-snapshot two-state model verifies 75% CF and 25% curtailment for a curtailed-then-uncurtailed solar generator). |
| `Done` | `Low` | `Frontend` | Analytics | Add load drill-down analytics | Loads drive system metrics but do not have a first-class detail view. | 8,000 | Rebuilt as `LoadAnalysisCard` with a system-vs-bus split. The four headline KPIs (system peak, system energy, **load factor** = avg/peak, **coincidence factor** = system peak / Σ bus peaks) capture demand-shape information that the existing system-load chart and duration curve don't quantify. Per-bus table is sorted by energy contribution and shows peak MW, energy MWh, load factor, and percentage share. Collapsible in the Result dashboard (default collapsed). |
| `Done` | `Low` | `Frontend` | Documentation | Auto-generate the support matrix from schema + capability declarations | Reduces drift between code and docs. | 9,000 | Fulfilled by `scripts/generate-support-matrix.mjs` (merges `src/config/pypsa_schema.json` with hand-edited `src/config/capabilities.json`) and the regenerable `docs/SUPPORT_MATRIX.md`. The components table is fully schema-driven (counts of input static / temporal / output attrs come from the schema; only the UI-side flags — detail card, map marker, notes — are hand-edited). Two flat lists for **Optimisation / scenario features** (single-period LOPF, pathway, rolling horizon, stochastic, SCLOPF, custom constraints, global_constraints editor, scenario presets, carbon price, load shedding, force-LP, annuitised CAPEX) and **Input / output formats** (xlsx Save/SaveAs, Project workbook, CSV folder, netCDF, HDF5, HTML report) keep status badges synchronised. Regenerate with `npm run generate:support-matrix`. |
| `Done` | `Medium` | `Frontend` | Types and standard types | Ship the PyPSA built-in `line_types` / `transformer_types` catalogues as a schema-driven config | Users had no easy way to discover PyPSA's 59 standard line types and 14 standard transformer types. PyPSA pre-populates them on every fresh `Network`, so the workbook needs **no** rows in `line_types` / `transformer_types` for them to resolve — Ragnarok just had to make the names selectable. | 6,000 | Fulfilled by `scripts/generate-pypsa-standard-types.mjs` (mirrors `generate-pypsa-schema.mjs` — fetches CSVs from `PyPSA/PyPSA` master, records commit SHA), `src/config/pypsa_standard_types.json` (regenerable JSON catalogue), and `src/constants/pypsa_standard_types.ts` loader. Surfaced in the UI as a `<datalist>` typeahead on the `lines.type` / `transformers.type` cells of the input table grid, with names merged from `model.line_types` / `model.transformer_types` (user-defined) and the built-in catalogue. Pinned by `src/constants/pypsa_standard_types.test.ts`. |
| `Done` | `High` | `Backend` | Backend correctness | Apply the `network` sheet explicitly in backend network construction | The `network` sheet was editable and preserved but previously not used as a true runtime input. | 8,000 | Fulfilled by adding a shared `network` runtime import policy, applying `network.name`, `srid`, `crs`, and `now` explicitly during backend `pypsa.Network` construction, and removing Ragnarok-side skip filtering for schema-defined sheets other than the real special cases `network` and `snapshots`. |
| `Done` | `Critical` | `Frontend` | Data integrity | Normalize all input dates to ISO (YYYY-MM-DD) at the import boundary | Date strings from user workbooks could be in dd/mm/yyyy, mm/dd/yyyy, or yyyy-mm-dd form. Without normalization the backend misparsed dates (e.g. 01-08-2024 read as January instead of August) and chart labels showed locale-specific strings instead of a stable canonical format. | 10,000 | Fulfilled by `normalizeDateToIso(raw, fmt)` in `src/shared/utils/helpers.ts` and `normalizeInputDatesToIso(model, fmt)` in `src/shared/utils/workbook.ts`. The user-facing **Date format** setting (`auto` / `dmy` / `mdy` / `ymd`) now **only** declares the format of the input data so the parser knows how to interpret ambiguous strings. All three import entry points in `App.tsx` call `normalizeInputDatesToIso` before any data reaches the state or the backend. The backend's `_snapshots_index` therefore always receives ISO and parses with no `dayfirst` override (which would re-flip already-ISO dates). The canonical target format everywhere — charts, slider, tables, exports — is `YYYY-MM-DD` (with `HH:MM` or `T HH:MM:SS` where time is present). |
| `Not Needed` | `High` | `Frontend` | Build experience | Add a **Topology** build mode alongside the existing step-by-step wizard (`Build — Serialised vs Topology` toggle) | The step-by-step (Serialised) wizard is good for guided, sheet-by-sheet authoring, but power users want a free-form canvas where the whole model is placed, connected, and edited directly on the map / network topology without stepping through sheets. | 26,000 | Major update #1. Builds directly on the Build map work already shipped: own-x/y component placement (`Done` row "Place point components at own x/y"), click-to-link buses (`Done` "Add click-to-link bus interaction"), "pick on map" attribute-form linking, and drag-to-move (commit `39efbfc`). Topology mode reuses `BuildNetworkMap` as the primary editing surface, the right-hand `BuildAttributeForm` for the selected component, and the same direct-to-`model` mutation API (`addRow` / `updateRowValue` / `bulkPaste` / `moveRow`); the Serialised wizard remains the default. Scope: toggle in the Build header; multi-bus click-linking for branches (lines/links/transformers); free placement of every component class on the canvas. **Not Needed:** deliberately dropped per user decision. The unified map-driven Build layout (commit `c593898`) already folds the intended free-form editing into the default Build experience — on-map component placement at own x/y, click-to-link buses, "pick on map" attribute linking, and drag-to-move — so a separate Serialised-vs-Topology toggle is redundant rather than a distinct mode. |
| `Done` | `High` | `Both` | Backend portability | Introduce a backend abstraction layer so the optimization backend is pluggable (PyPSA today, others later) | Ragnarok's frontend, builder, and result derivation are hardwired to PyPSA. A thin adapter interface (`model → backend network → solve → schema-driven outputs`) would let alternative backends be added without rewriting the frontend. Prerequisite for any non-PyPSA backend. | 30,000 | Major update #2. Fulfilled with a single-method seam: a new `backend/app/backends/` package defines a `Backend` protocol (`name`, `label`, `capabilities()`, `run(model, scenario, options)`), a `registry` (`get_backend` / `available_backends` / `register_backend`, default `pypsa`), and `PypsaBackend` — a thin adapter wrapping the existing `run_pypsa` end-to-end pipeline unchanged. `build_network` stays an internal PyPSA detail (also reused by the netCDF/HDF5 file converters), not part of the seam. The "what to do" metadata is the existing `options` dict (pathway / rolling / stochastic / SCLOPF / force-LP); `options["backend"]` selects the adapter. `main.py`'s solve worker resolves the backend from the registry, `/api/run` fails fast with HTTP 400 on an unknown backend, and a new `GET /api/backends` lists capabilities. Frontend threads `options.backend = 'pypsa'`. Server stays stateless. Pinned by `backend/tests/test_backends.py` (default/case-insensitive resolution, unknown-name `BackendError`, protocol conformance, capabilities, and an adapter-vs-`run_pypsa` result-contract parity check). |
| `Open` | `Medium` | `Both` | Optimization capability | Add a power-flow-only (PF / linear PF) study mode separate from the optimization workflow | Every Ragnarok run today is an optimization. PyPSA supports pure power-flow studies (`network.pf()` / `network.lpf()`); a non-optimization PF mode broadens Ragnarok to network-analysis use cases and pairs naturally with the topology builder. | 16,000 | Recommended addition. Currently listed as `Not supported` in the README support matrix ("Power flow-only studies / separate PF UX"). |
| `Done` | `High` | `Frontend` | Analytics | Adaptive x-axis datetime labels and tick density for time-series charts | Chart x-axis showed only `HH:MM` regardless of how many days were visible, making multi-day ranges unreadable. Tick density was fixed at ~8 ticks no matter the span. | 6,000 | Fulfilled in `src/features/analytics/cards/InteractiveTimeSeriesCard.tsx`. A `formatXLabel(ts, spanMs)` helper selects the appropriate ISO sub-format based on the visible span: `HH:MM` (≤ 24 h), `YYYY-MM-DD HH:MM` (24 h – 7 d), `YYYY-MM-DD` (7 d – 90 d), `YYYY-MM` (> 90 d). Tick stride is also span-driven (`targetTicks` from 12 down to 8) so tick density scales with the data window rather than always showing ~8 ticks. Both the chart and the timeline slider use the same ISO local-component helpers (`isoDate`, `isoTime`) so they never shift the calendar day in non-UTC timezones. |

## Major upcoming directions

These are the three large bodies of work the project is steering toward. They are tackled one at a time.

1. **Topology build mode** (`Serialised vs Topology` toggle) — a full free-form map/network builder as an alternative to the step-by-step wizard. *(In planning; first of the three.)*
2. **Pluggable backend** — a backend abstraction layer so PyPSA is one reference adapter among several. *(Deferred — larger effort, after the topology builder.)*
3. **Data platform** — the existing data-platform cluster below (location-based renewable profiles, profile database layer, national starter models, source registry, health/provenance, country import presets) is this program.

## Suggested execution order

1. ~~Introduce the backend abstraction layer for pluggable backends (major update #2).~~ **Done** — seam landed; a concrete second adapter is future work.
2. Add the Topology build mode (major update #1).
3. Build the data platform: location-based renewable profile import, the supporting data registry/database layer, and national-level starter model imports (major update #3).
4. Add a power-flow-only study mode (deferred).
5. Expand scenario-aware analytics and remaining component-specific result UX after the project exchange path, data layer, and core optimization modes are trustworthy.
